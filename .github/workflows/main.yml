name: Trucker
on: [push]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js 
        uses: actions/setup-node@v1
        with: 
          node-version: 20
      - run: npm  install
      - run: npm run check
